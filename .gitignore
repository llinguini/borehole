# Rust build artifacts
/target
**/*.rs.bk

# Cargo.lock is intentionally tracked: this workspace ships binaries.

# Local TLS material and runtime secrets (never commit these).
# The committed sample stays via the explicit negation below.
*.pem
config.json
!borehole.example.json

# Docker local-stack runtime material: certs auto-generated by the `certgen`
# service and any local runtime config dropped next to the compose file.
# `docker/config/node.json` is a tracked sample and survives via the negation.
docker/certs/
docker/config/config.json

# Editor / IDE
.idea/
*.swp
*.swo

# OS noise
.DS_Store
Thumbs.db

CHANGELOG.md
project-knowledge.md